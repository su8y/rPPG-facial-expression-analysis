import {Card, Center, Text, Title} from "@mantine/core";interface DetailSectionProps {    title: string;    description: string;    information: string;}export function DetailSection({title, description, information, children}: DetailSectionProps) {    return <Card withBorder radius={'md'}>        <Title order={5}>{title}</Title>        <Text size={'xs'} c="dimmed">{description}</Text>        {children}        <Center>            <Text size={'xs'}  style={{whiteSpace: 'pre-line'}}>{information}</Text>        </Center>    </Card>;}