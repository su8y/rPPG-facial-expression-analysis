import {Box, Button, Center, Container, Flex, Stack, Text} from "@mantine/core";interface ErrorDisplayProps {    title: string;    message: string;    onRetry: () => void;}const WarningIcon = () => (    <svg        width="24"        height="24"        viewBox="0 0 24 24"        fill="none"        xmlns="http://www.w3.org/2000/svg"    >        <path            fillRule="evenodd"            clipRule="evenodd"            d="M12 2C6.48 2 2 6.48 2 12C2 17.52 6.48 22 12 22C17.52 22 22 17.52 22 12C22 6.48 17.52 2 12 2ZM12.75 16V14.5H11.25V16H12.75ZM11.25 8V13H12.75V8H11.25Z"            fill="#D9534F" // 에러 붉은색        />    </svg>);/** * 데이터 로드 실패 시 표시되는 인라인 에러 컴포넌트 * @param {string} title - 로드에 실패한 항목의 이름 (예: "토스뱅크") * @param {string} message - 표시할 에러 메시지 (기본값: "불러올 수 없어요") * @param {function} onRetry - "다시 시도" 버튼 클릭 시 실행될 함수 */export const ErrorDisplay = ({                                 title = "불러오기 실패",                                 message = "불러올 수 없어요",                                 onRetry,                             }: ErrorDisplayProps) => {    return (        <Container>            <Center>                <Box>                    <Stack>                        <Flex gap={'xs'} align={'center'}>                            <WarningIcon/><Text size={'lg'}>{title}</Text>                        </Flex>                        <Text c={'red'}>{message}</Text>                    </Stack>                </Box>                <Button variant="white" onClick={onRetry}>다시시도</Button>            </Center>        </Container>    );};